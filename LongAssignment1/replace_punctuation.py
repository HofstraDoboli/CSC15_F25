'''
 Implement the replace_punctuation() function. 
 replace_punctuation() has a string parameter 
 and two keyword argument parameters exclamation_count and semicolon_count. 
 replace_punctuation() updates the string by replacing each exclamation point (!) 
 character with a period (.) and each semicolon (;) character with a comma (,). 
 replace_punctuation() also counts the number of times each character is 
 replaced and outputs those counts. Lastly, replace_punctuation() returns the updated string. 
'''

def replace_punctuation(text, exclamation_count = 0, semicolon_count = 0):
    new_text = ""
    
    print('Exclamation count:', exclamation_count)
    print('Semicolon count:', semicolon_count)
    return new_text

# how do you call this function in execute_menu function:
# updated_text = replace_punctuation(user_text) 
